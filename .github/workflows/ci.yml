name: Secure CI Pipeline

on:
  push:
  pull_request:

jobs:
  security-checks:
    runs-on: ubuntu-latest
    # Permissions lazmi hain taake Gitleaks aur CodeQL sahi se kaam karein
    permissions:
      contents: read
      security-events: write
      pull-requests: read

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0 # Gitleaks ke liye poori history scan karna behtar hai

    # -------------------------------
    # 🔍 CodeQL: Static code analysis
    # -------------------------------
    - name: Initialize CodeQL
      uses: github/codeql-action/init@v3
      with:
        languages: javascript-typescript # Combine languages for modern setup

    - name: Autobuild (CodeQL)
      uses: github/codeql-action/autobuild@v3

    - name: Run CodeQL Analysis
      uses: github/codeql-action/analyze@v3

    # -------------------------------
    # 🔍 Gitleaks: full filesystem scan
    # -------------------------------
    - name: Run Gitleaks
      uses: gitleaks/gitleaks-action@v2
      with:
        args: detect --source . --no-git --redact --verbose --exit-code 1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    # -------------------------------
    # 🔍 Trivy: Vulnerability scan
    # -------------------------------
    - name: Run Trivy (filesystem scan)
      uses: aquasecurity/trivy-action@master
      with:
        scan-type: 'fs'
        severity: 'CRITICAL,HIGH'
        exit-code: '1'

    # -------------------------------
    # Node.js setup + dependencies
    # -------------------------------
    - name: Install Node.js
      uses: actions/setup-node@v4 # Updated to v4
      with:
        node-version: '20'
        cache: 'npm' # Build fast karne ke liye caching zaroori hai
        cache-dependency-path: |
          backend/package-lock.json
          frontend/package-lock.json

    - name: Install Dependencies (Backend)
      run: |
        cd backend
        npm ci

    - name: Install Dependencies (Frontend)
      run: |
        cd frontend
        npm ci

